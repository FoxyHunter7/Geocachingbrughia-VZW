# Geocaching Brughia - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================
# BACKEND CONFIGURATION (used by docker-compose)
# =============================================

# JWT Authentication
# IMPORTANT: Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-secret

# SMTP Configuration (for contact form notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notifications@geocachingbrughia.be
SMTP_PASS=your-smtp-password
SMTP_FROM=Geocaching Brughia <notifications@geocachingbrughia.be>

# Email address to receive contact form submissions
NOTIFICATION_EMAIL=admin@geocachingbrughia.be

# Days before sending a reminder for unanswered contact forms
REMINDER_DAYS=3

# CORS - Comma-separated allowed origins
# For Docker local testing: http://localhost
# For production: https://geocachingbrughia.be,https://www.geocachingbrughia.be
CORS_ORIGINS=http://localhost

# =============================================
# FRONTEND CONFIGURATION (Vite build-time vars)
# =============================================
# These are in frontend/.env and frontend/.env.production
# because Vite bakes them into the JS bundle at build time.
#
# Local dev (npm run dev):     VITE_API_URL=http://localhost:8080/api/
# Docker local:                VITE_API_URL=http://localhost/api/
# Production:                  VITE_API_URL=https://api.geocachingbrughia.be/api/
#
# To test with Docker locally, update frontend/.env before building:
#   VITE_API_URL=http://localhost/api/

